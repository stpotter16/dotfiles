#!/usr/bin/env bash

# Fail on first error
set -e

# Fail on unset variables
set -u

# Echo commands
set -x

# Destroy container, destroy volume
docker container rm $TRADER_API_PG_CONTAINER

docker volume rm $TRADER_API_PG_VOLUME
